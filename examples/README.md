# Examples

`daywise_session_manifest.csv` is a small synthetic legacy-format example. It is not a project
catalog and its dates must not be treated as global defaults. Project manifests and preparation
plans are generated with `tools/build_session_manifest.py` for an explicit mouse and laser.
